<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create(config('messenger.tables.attachments', 'messenger_message_attachments'), function (Blueprint $table) {
            $table->ulid('id')->primary();

            $table->ulid('message_id')->index();

            $table->string('disk');
            $table->string('path');
            // Original client file name, preserved for display/download.
            $table->string('name');
            $table->string('mime_type');
            $table->string('extension')->nullable();
            // File size in bytes.
            $table->unsignedBigInteger('size')->default(0);

            $table->timestamps(6);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists(config('messenger.tables.attachments', 'messenger_message_attachments'));
    }
};
